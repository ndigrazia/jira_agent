[project]
name = "adk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-adk[extensions,a2a]>=2.3.0",
    "a2a-sdk>=0.3.4",
    "load-dotenv>=0.1.0",
    "mcp>=1.28.1",
]
