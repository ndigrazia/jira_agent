version: '3.8'

services:
  jira-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jira-agent-a2a-container
    ports:
      - "8081:8081"
    environment:
      - PORT=8081
    env_file:
      - .env
    restart: unless-stopped
