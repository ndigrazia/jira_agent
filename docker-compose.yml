version: '3.8'

services:
  jira-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jira-agent-a2a-container
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    env_file:
      - .env
    restart: unless-stopped
